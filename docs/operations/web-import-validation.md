# Chumji News Web Import Validation

Validation was run from branch `sw326/issue-13-web-preserve` after importing `sw326/chumji-news` into `apps/web/`.

## Commits

- `cebf5de` - removed the placeholder `apps/web/README.md` so the subtree could own the prefix.
- `ea2c68d` - imported committed `chumji-news` history with `git subtree add --prefix=apps/web chumji-news-src/main`.
- `8a3000e` - preserved the source checkout's dirty tracked diff and untracked non-ignored files under `apps/web/`.
- `4097bf3` - documented import guardrails.

## Source Preservation Check

The source checkout `/Users/chumji/workspace/chumji-news` was not modified. Its post-import status still matched the audited dirty state:

```text
## main...origin/main
 M src/components/NewsBoardClient.tsx
 M src/lib/data.ts
 M src/lib/types.ts
?? public/
?? scripts/save-price-snapshot.js
?? src/app/prices/
?? src/components/MainTabs.tsx
```

## Local Validation

Commands run:

```bash
cd apps/web
npm ci
npm run lint
npm run build
```

Results:

- `npm ci` completed successfully. It reported package audit findings: 1 low and 7 high vulnerabilities.
- `npm run lint` failed on imported script files because the configured ESLint rules forbid CommonJS `require()` in JavaScript utility scripts.
- `npm run build` completed successfully with Next.js 16.2.1 and generated routes for `/`, `/news/[date]/[cat]`, `/prices`, and `/prices/[date]`.

Lint findings were not fixed in this import task to keep the preserved source application behavior unchanged.

## Secret Scan

A local tracked-file scan checked for:

- tracked non-example `.env` files
- private key blocks
- AWS access key shaped values
- OpenAI key shaped values
- JWT-shaped values
- non-placeholder env assignments

Result: no findings.

## Worktree Check

After validation, the only local untracked/ignored artifacts were generated by local install/build:

```text
!! apps/web/.next/
!! apps/web/next-env.d.ts
!! apps/web/node_modules/
```

No production deploy, Vercel relink, Supabase mutation, scheduler change, or live path write was performed.
